# from http://cuppster.com/2011/05/18/using-supervisor-with-upstart/
description     "supervisor"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
expect fork

exec <%= node[:graphite][:venv] %>/bin/supervisord --nodaemon --configuration /etc/supervisord.conf
